{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "curl -s -o /dev/null -w \"%{http_code}\" http://localhost:3000 2>/dev/null | grep -q 200 || (cd \"C:/slef learning\" && node src/index.js serve &> /dev/null &)"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"process.stdin.on('data',d=>{const j=JSON.parse(d);const f=j.tool_input?.file_path||j.tool_response?.filePath||'';console.log(f)})\" | grep -E '\\.js$' && npm test || true"
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Bash(npm *)",
      "Bash(node *)",
      "Bash(git *)",
      "Bash(curl *)"
    ],
    "deny": [
      "Bash(rm -rf *)",
      "Bash(git push --force *)",
      "Bash(git reset --hard *)",
      "Bash(sudo *)",
      "Bash(curl * | bash)",
      "Bash(wget * -O - | sh)"
    ]
  }
}
